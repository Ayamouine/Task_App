/*document.addEventListener('DOMContentLoaded', function() {
    // Exemple de données de projets assignés (à remplacer par une requête AJAX vers le serveur)
    const assignedProjects = [
        { id: 1, name: "Projet 1", description: "Description du projet 1" },
    ];

    const assignedProjectsList = document.getElementById('assigned-projects');

    assignedProjects.forEach(project => {
        const li = document.createElement('li');
        li.innerHTML = `<strong>${project.name}</strong> - ${project.description}`;
        assignedProjectsList.appendChild(li);
    });
});*/